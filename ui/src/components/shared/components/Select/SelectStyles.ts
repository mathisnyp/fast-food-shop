import { styled } from "../../../../shared/styles"

export const Select = styled("select", {
  maxWidth: "10em",
  border: "1px solid",
  fontSize: "1.25rem",
  cursor: "pointer",
  lineHeight: "1.1",
  backgroundColor: "$background",
  borderRadius: "0.25em",
})
